class Method {
  /**
   * Method
   * @param {string} name
   * @param {Object} options
   */
  constructor (name, options) {
    this.name = name
    this.options = options
  }
}

module.exports = Method
